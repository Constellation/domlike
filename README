
simple DOMLike Parser for AppJet

いまのところFirefoxでしかためしてない.
AppJetがJS verいくらなのかわかんないので調べて調製するつもり.

usage


var dom = new DOM();
var doc = dom.parseFromString(res.responseText);
log(doc);//=> DOMLike Object
// example
var app = doc.getElementById('app');
var divs = doc.getElementsByTagName('div');
